import {NextResponse} from "next/server";import {randomBytes} from "crypto";import {metaLoginUrl} from "../../../../lib/meta";export async function GET(){const state=randomBytes(24).toString("hex");const res=NextResponse.redirect(metaLoginUrl(state));res.cookies.set("meta_oauth_state",state,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:600,path:"/"});return res}